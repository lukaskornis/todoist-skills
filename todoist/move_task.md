python move_task.py <task_id> [--section ID] [--project ID] [--parent ID]
# at least one flag required; --parent moves to same project automatically
# to move out of section to root: --project with same project id
→ {id content priority project_id due labels description}
! 404 ids not found
